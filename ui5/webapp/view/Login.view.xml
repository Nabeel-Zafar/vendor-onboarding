<mvc:View
    xmlns="sap.m"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns:l="sap.ui.layout"
    xmlns:core="sap.ui.core"
    xmlns:html="http://www.w3.org/1999/xhtml"
    controllerName="vendor.onboarding.controller.Login"
    displayBlock="true">
    <Page showHeader="false" class="loginPage">
        <content>
            <FlexBox justifyContent="Center" alignItems="Center" height="100%">
                <VBox alignItems="Center" class="loginBox">
                    <Image
                        src="images/IXOM+Transparent.webp"
                        width="200px"
                        class="sapUiMediumMarginBottom loginLogo" />
                    <Title text="{i18n>loginTitle}" level="H2" class="sapUiSmallMarginBottom" />
                    <l:VerticalLayout class="sapUiMediumMarginTop" width="100%">
                        <Input
                            id="usernameInput"
                            placeholder="{i18n>loginUsername}"
                            class="sapUiSmallMarginBottom"
                            width="100%" />
                        <Input
                            id="passwordInput"
                            type="Password"
                            placeholder="{i18n>loginPassword}"
                            class="sapUiSmallMarginBottom"
                            width="100%"
                            submit="onLogin" />
                        <Button
                            text="{i18n>loginButton}"
                            type="Emphasized"
                            width="100%"
                            press="onLogin"
                            class="sapUiSmallMarginTop" />
                    </l:VerticalLayout>
                </VBox>
            </FlexBox>
        </content>
    </Page>
</mvc:View>